#include<stdio.h>
       void main()
{
    int i,n,a,b,k,p;
     printf("Enter the initial and ending no of tables:");
     scanf("%d%d",&a,&b);
     printf("Enter the initial and ending no of tables:");
     scanf("%d%d",&k,&p);
for
     (n=a;n<=b;n++)
{
for
     (i=k;i<=p;i++)
{
     printf("%d*%d=%d\n",n,i,n*i);
}

}
}
